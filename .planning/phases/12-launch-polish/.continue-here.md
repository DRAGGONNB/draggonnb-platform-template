---
phase: 12-launch-polish
wave: 3 of 4 (Wave 2 + Wave 4 parked at user's request)
plan: 12-06 (in_progress, ~50% done)
total_plans: 10 (1 done, 1 in-progress, 8 not started)
status: in_progress
last_updated: 2026-04-29 (session paused — user moving locations)
---

<current_state>

User paused mid-Wave-3 because they're disconnecting and moving to a different
location. The 12-06 (dynamic sidebar) executor agent died with `ConnectionRefused`
after 45 tool calls. Substantial component work landed on disk uncommitted —
captured as WIP commit `d5d1e7fe` so nothing's lost.

**Production is stable on `157b297b`** (Wave 1 hotfixes deployed and verified).

**The WIP commit `d5d1e7fe` is NOT pushed** and should be amended/finished, not
promoted as-is. Sidebar links in the partial work will 404 until the new
top-level routes are built.

</current_state>

<completed_work>

**Wave 1 — Hotfixes (12-01): COMPLETE and DEPLOYED to production**
- A1 CRM new-contact crash → fixed (try/catch around `loadEasyViewData`)
- A2 Autopilot generate fails → resolved by Anthropic credit funding
- A3 Social media tab bug → fixed (sidebar `/social` rerouted to `/settings/social`)
- A4 Brand voice wizard fails → resolved by Anthropic credit funding
- A5 Lead Scoring `/crm/scoring` bombs → fixed (placeholder page added)
- A6 Sidebar active-state on sub-routes → fixed (`pathname.startsWith` semantics)
- A7 Campaign drafter fails → resolved by Anthropic credit funding
- AI error surfacing → fixed (BaseAgent typed `ANTHROPIC_INSUFFICIENT_CREDITS` error)
- Commits: `a1778d97`, `88ac4d10`, `7e1f9ea4`, `157b297b` (retrigger)
- Production deploy: `dpl_8g4qVE7pY2gBxAGvcX11mdZtSytN` (READY)

**Wave 3 — Plan 12-06 dynamic sidebar (PARTIAL, captured as WIP `d5d1e7fe`):**
- NEW files (8): `sidebar-server.tsx`, `sidebar-client.tsx`, `sidebar-section.tsx`,
  `sidebar-flyout.tsx`, `mode-toggle.tsx`, `lib/dashboard/build-sidebar.ts`,
  + 2 test files
- MODIFIED (3): old `Sidebar.tsx` stripped (-255 lines), `layout.tsx` (-32),
  `sidebar.test.tsx` (-143)

</completed_work>

<remaining_work>

**Wave 3 — what's left:**

### 12-06 (sidebar) — finish the partial work
1. Verify tsc clean on the new components — agent never reached its gate.
   Run `npx tsc --noEmit` and fix any errors.
2. Run vitest on the new tests:
   - `__tests__/components/dashboard/sidebar-build.test.ts`
   - `__tests__/components/ui/mode-toggle.test.tsx`
   - Plus the modified `sidebar.test.tsx`.
3. **Build the new top-level overview routes** that don't exist yet but the
   new sidebar links to:
   - `/content-studio` (NEW) — minimal hero + quick-links to existing
     `/content-generator`, `/email/*`, `/campaigns/*`, `/social/*`
   - `/customers` (NEW) — minimal hero + quick-links to `/crm`, `/crm/advanced`,
     `/crm/scoring`
   - `/insights` (NEW) — minimal hero + quick-links to `/analytics`,
     `/admin/cost-monitoring`
   - `/settings` — verify exists and links work
   - `/operations` — NOT NEEDED (per CONTEXT, verticals get their own
     top-level entries; Operations as a wrapper was rejected)
4. Smoke-test in the browser locally (`pnpm run dev`) — log in as
   `tester-admin@draggonnb.test` (DragoonB org has all modules) and verify:
   - Sidebar shows 6+ items reordered with verticals first
   - Hover flyout panel works
   - Click any sidebar item navigates without 404
   - Active state highlights correctly on sub-routes (A6 fix preserved)
5. Write `.planning/phases/12-launch-polish/12-06-SUMMARY.md`.
6. Final commit: `docs(12-06): complete dynamic sidebar plan`.
7. Push to origin/main when verified — triggers Vercel production rebuild.

### 12-07 (smart-landing dashboard) — not started
Depends on 12-06 finishing. Rebuilds `/dashboard/page.tsx` to surface
ONE big "today's quick action" card at the top, derived from tenant state
(cross-module). Mockup reference: `docs/redesign/dashboard-preview.html`
and scenes 1+2 of `docs/redesign/full-experience-mockup.html`.

### 12-08 (module-focused public landing) — not started
Independent of 12-06/12-07 (depends on 12-02 which is parked, but content
sources from `docs/modules/*.md` are already done). Could run in parallel
with 12-07 once 12-06 ships.

**Parked at user's explicit request 2026-04-29:**
- Wave 2 — 12-02 audit, 12-03 activated_at migration, 12-04 ops crons, 12-05
  mobile sweep. User: "cron stuff I'm not too concerned about at the moment.
  I just want a functional UI."
- Wave 4 — 12-09 floating helper, 12-10 site concierge. User: "I'm happy
  with [the helper], it's not critical at this stage."

</remaining_work>

<decisions_made>

**Wave priority flip (2026-04-29):** User explicitly prioritised Wave 3
(visible UX redesign) over Wave 2 (audit + crons). Reasoning, verbatim:
*"people seeing our initial dashboard and sidebars and thinking what the
fuck did I just buy. They need something simple, something more related and
something easy."* This is a commercial conversion concern, not polish.

**Sidebar IA decisions locked (from CONTEXT.md and refined visually):**
- 6-7 top-level items max (was 54)
- Order: Dashboard → activated verticals (by `enabled_at` date) → Content
  Studio → CRM → Insights → Settings → [Admin if platform_admin]
- Sub-pages become in-page horizontal tabs, NOT sidebar items
- Hover flyout for power-user quick-jump (per `full-experience-mockup.html`
  scene 5)
- DB-driven from `tenant_modules` + `module_registry` join (NOT hardcoded)
- Mode toggle = "Autopilot ⇄ Hands-on" (per Chris naming preference)
- Sidebar palette: `#1E1B16` charcoal · `#B8941E` gold accent · `#FDFCFA`
  warm white (Trophy OS-aligned warm dark)

**Smart-landing dashboard:** action-first not metrics-first. Top of page
is ONE big "today's quick action" card with Autopilot ⇄ Hands-on buttons.
Metrics still available, just below the fold.

**Telegram = mobile** (architectural confirmation): no native iOS/Android
app. Phase 12 Wave 4 (parked) ties Telegram approval flow to a floating
AI helper on web.

</decisions_made>

<blockers>

- **Vercel webhook flakiness:** webhook silently dropped pushes twice in
  this session (the merge commit + the hotfix push). Workaround: empty
  retrigger commit. Worth flagging as ops concern but not blocking.

- **GitHub API rate-limit was hit** during the last poll loop. Wait until
  next session to use `api.github.com` again, or use Vercel MCP instead
  (which we proved is reliable).

- **WIP commit `d5d1e7fe`** has untested code. Resume must run tsc + vitest
  before pushing.

</blockers>

<context>

**The pattern of this session:** user did a production test pass, found
issues, prioritised UX redesign over backend ops work. Wave 1 hotfixes
shipped. Wave 3 is the high-visibility one that turns the dashboard into
the "smart landing" experience from the mockups in `docs/redesign/`.

**What "done" looks like for Wave 3:**
1. Sidebar shows 6 items not 54
2. Hover any item → flyout panel with sub-pages
3. Click any item → lands on a page (no 404s)
4. Top of `/dashboard` shows ONE big quick-action card — "Generate this
   week's content" or similar — with Autopilot/Hands-on buttons
5. Public landing page features modules with what each actually does
6. Production deploy goes green on `draggonnb.online`

**Test creds (still valid):** all three users have password
`DraggonTest2026!`:
- `tester-admin@draggonnb.test` (DragoonB org `094a610d-2a05-44a4-9fa5-e6084bb632c9`
  — has all 11 modules activated, perfect for visual sidebar testing)
- `tester-pro@draggonnb.test` (FIGARIE)
- `tester-starter@draggonnb.test` (chrisctserv's Org)

**Mockup references** to use as visual targets:
- `docs/redesign/full-experience-mockup.html` — 5 scenes covering all
  scenarios (lodge / restaurant / base content studio / CRM / dropdown)
- `docs/redesign/dashboard-preview.html` — full-screen dashboard sample
- `docs/redesign/sidebar-mockup.html` — earlier v2 reference

**Phase 12 plans on disk** at `.planning/phases/12-launch-polish/`:
12-01 (done), 12-02..12-10 ready to execute. Wave 3 = 12-06, 12-07, 12-08.

**Anthropic credits funded.** Build/runtime AI calls work.

**Production state at pause:**
- `main` branch: WIP at `d5d1e7fe` (sidebar partial)
- `origin/main`: at `157b297b` (Wave 1 hotfixes — last successful production deploy)
- Production URL: `https://draggonnb.online` serves `157b297b` cleanly

</context>

<next_action>

**On resume, do this in order:**

1. **Open a fresh session** — `/clear` first, the context window for this
   session is heavy.

2. **Run `/gsd:resume-work`** — should pick up this `.continue-here.md`
   automatically.

3. **Verify the WIP commit is what we expect:**
   ```bash
   git log -1 --stat d5d1e7fe
   git status   # should show clean (no uncommitted work)
   ```

4. **Decide on the 12-06 finishing approach:**
   - **Option A (recommended):** spawn a fresh `gsd-executor` agent for 12-06
     with explicit instructions to start from the WIP commit and finish:
     verify tsc, build the 3 new overview routes (`/content-studio`,
     `/customers`, `/insights`), write SUMMARY, push.
   - **Option B:** finish 12-06 manually in main session — slower but more
     visibility. Pick this if previous agent failures suggest API instability.

5. **After 12-06 is on production:**
   - Test on production with `tester-admin@draggonnb.test`
   - Verify sidebar shows 6 items, flyout works, no 404s
   - If green, spawn 12-07 + 12-08 in parallel (they're independent now
     that 12-06 is done — 12-08's dependency on 12-02 audit is bypassable
     since content audit was done in this session via `docs/modules/*.md`)

6. **DO NOT touch:**
   - Wave 2 (12-02..05) — parked
   - Wave 4 (12-09..10) — parked

</next_action>
