# =============================================================================
# DraggonnB CRMM - Docker Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the values
#
# IMPORTANT: Never commit .env to version control!
# =============================================================================

# =============================================================================
# GENERAL SETTINGS
# =============================================================================

# Your domain (without protocol)
DOMAIN=draggonnb.app

# Timezone (South Africa)
TIMEZONE=Africa/Johannesburg

# =============================================================================
# TRAEFIK SETTINGS
# =============================================================================

# Email for Let's Encrypt SSL certificates
ACME_EMAIL=admin@draggonnb.app

# Traefik dashboard credentials
# Generate with: docker run --rm httpd:2.4-alpine htpasswd -nbB admin 'your-secure-password'
# Format: username:hashed-password
TRAEFIK_DASHBOARD_CREDENTIALS=admin:$$apr1$$xyz123$$hashedpassword

# =============================================================================
# N8N SETTINGS
# =============================================================================

# N8N subdomain (will be: n8n.draggonnb.app)
N8N_SUBDOMAIN=n8n

# N8N authentication
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=your-secure-password-here

# N8N encryption key (IMPORTANT: Generate once and never change)
# Generate with: openssl rand -hex 32
N8N_ENCRYPTION_KEY=your-64-character-hex-encryption-key-here

# Database type: sqlite (default) or postgresdb
N8N_DB_TYPE=sqlite

# N8N log level: debug, info, warn, error
N8N_LOG_LEVEL=info

# External hooks (optional)
N8N_EXTERNAL_HOOKS=

# =============================================================================
# POSTGRESQL SETTINGS (Optional - if using PostgreSQL instead of SQLite)
# =============================================================================

# Uncomment and configure if using PostgreSQL
# POSTGRES_USER=n8n
# POSTGRES_PASSWORD=your-postgres-password
# POSTGRES_DB=n8n

# =============================================================================
# BACKUP SETTINGS
# =============================================================================

# Backup schedule (cron format, default: daily at 2 AM)
BACKUP_CRON=0 2 * * *

# Backup retention in days
BACKUP_RETENTION_DAYS=7

# External backup path (for additional backup location)
BACKUP_EXTERNAL_PATH=./backups

# Notification URLs (optional, for backup alerts)
# Supports: Slack, Discord, Email, etc.
# Format: slack://hook.slack.com/services/xxx/yyy/zzz
BACKUP_NOTIFICATION_URLS=

# =============================================================================
# OPTIONAL: CLOUD BACKUP DESTINATIONS
# =============================================================================
# For S3-compatible storage (AWS S3, DigitalOcean Spaces, etc.)

# AWS_S3_BUCKET_NAME=your-bucket-name
# AWS_S3_PATH=backups/draggonnb
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_ENDPOINT=https://s3.amazonaws.com
# AWS_REGION=af-south-1

# =============================================================================
# FUTURE SERVICES
# =============================================================================

# Claude Orchestrator API Key (Phase 6)
# CLAUDE_API_KEY=sk-ant-...

# Supabase Service Role Key (for client provisioning)
# SUPABASE_SERVICE_ROLE_KEY=...

# =============================================================================
# SECURITY NOTES
# =============================================================================
#
# 1. Use strong, unique passwords for all services
# 2. Generate encryption keys with: openssl rand -hex 32
# 3. Rotate credentials periodically
# 4. Never share this file or commit to version control
# 5. Use environment-specific .env files (.env.staging, .env.prod)
#
# =============================================================================
